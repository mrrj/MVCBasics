@{
    ViewData["Title"] = "Guessing Game";
}

<h1>Guessing Game</h1>

<p>Make a guess for a number between 1 and 100.</p>

<form asp-action="GuessingGame" method="post">
    <input type="number" id="guess" name="guess" min="1" max="100" />
    <input type="submit" value="Submit">
</form>

@if(ViewBag.Message != null)
            {
                <p>@ViewBag.Message</p>
            }